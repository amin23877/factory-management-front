stages:
  - build

build:
  stage: build
  image:
    name: node:lts-stretch
    entrypoint: [""]
  # rules:
  #   - changes:
  #     - src/*
  before_script:
    - apt-get -qq update && apt-get -qq install -y zip unzip sshpass
    - npm i
  script:
    - echo "export const host = 'https://ts.digitalphocus.ir'" > src/host.ts
    - NODE_OPTIONS=--max_old_space_size=1000; npm run build
    - zip -r build.zip build
    # - mkdir build
    # - ls
    # - which ssh
    # - which scp
    - "which ssh-agent || ( apt-get update -y && apt-get install openssh-client -y )"
    - eval $(ssh-agent -s)
    - ssh-add <(echo "$SSH_PRIVATE_KEY")
    - mkdir -p ~/.ssh
    - '[[ -f /.dockerenv ]] && echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config'
    - scp build.zip root@185.110.191.89:/home/phocus.frontend/build.zip
    - ssh root@185.110.191.89 "cd /home/phocus.frontend; rm -rf build; unzip -A build.zip"
